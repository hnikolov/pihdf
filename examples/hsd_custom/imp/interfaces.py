from myhdl import *

my_port_fields = [("cmd", bool(0)),
                  ("port", intbv(0)[16:])]
#-----------------------------------------------------------

other_fields = [("fld1", intbv(2, min=-128, max=256)),
                ("fld2", intbv(0)[8:])]
#-----------------------------------------------------------

